{
  "name": "common-lambda-layer",
  "version": "1.0.0",
  "scripts": {
    "copy": "cp -r node_modules nodejs",
    "deploy:quick:prod": "serverless deploy --stage prod",
    "deploy:quick:test": "serverless deploy",
    "deploy:test": "serverless remove && npm install --production && npm run copy && npm run zip && npm run deploy:quick:test",
    "deploy:prod": "serverless remove --stage prod && npm install --production && npm run copy && npm run zip && npm run deploy:quick:prod",
    "zip": "zip -r nodejs.zip nodejs"
  },
  "author": "author",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.0",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "helmet": "^3.18.0",
    "request": "^2.88.0"
  }
}
