{
  "name": "common-lambda-layer",
  "version": "1.0.0",
  "scripts": {
    "copy": "cp -r node_modules nodejs",
    "deploy:quick:prod": "serverless deploy --stage prod",
    "deploy:quick:test": "serverless deploy",
    "deploy:test": "npm install --production && npm run copy && npm run zip && npm run deploy:quick:test",
    "deploy:prod": "npm install --production && npm run copy && npm run zip && npm run deploy:quick:prod",
    "zip": "zip -r nodejs.zip nodejs"
  },
  "author": "author",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.0",
    "puppeteer": "^1.18.1"
  }
}
